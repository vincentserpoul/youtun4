---
# yamllint configuration
extends: default

rules:
  # GitHub Actions SHA-pinned action references legitimately exceed 80 chars
  line-length:
    max: 120
    level: warning

  # Allow single space before inline comments (e.g. `@<sha> # v1.2.3`)
  comments:
    min-spaces-from-content: 1
